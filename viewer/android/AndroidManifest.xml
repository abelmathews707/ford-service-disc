<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (c) 2026 Alexander Hill. SPDX-License-Identifier: MIT -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fordservicedisc.viewer"
    android:versionCode="2"
    android:versionName="1.1">

    <!-- The wrapper serves the site from http://127.0.0.1; requests are
         answered by shouldInterceptRequest, so no real network is used. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Needed to read the manual from /sdcard/FordManual on Android 10/11. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false">
        <activity android:name=".MainActivity" android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
